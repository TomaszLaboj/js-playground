const divideReturnRemainder = (a, b) => {
    const divisionRemainder = a % b;
    return `The remainder of dividing ${a} by ${b} is ${divisionRemainder}`;
};

console.log(divideReturnRemainder(21,4));
